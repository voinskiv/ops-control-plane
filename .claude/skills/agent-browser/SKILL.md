---
name: agent-browser
description: Browser automation and visual verification for this repository's UI.
allowed-tools: Bash(agent-browser:*), Bash(npx agent-browser:*)
---

# agent-browser

## Start here

Before running `agent-browser`, load the current workflow from the installed
CLI:

```bash
agent-browser skills get core
```

Run `agent-browser skills list` to see other available skills (electron, slack, sandbox, etc.).
